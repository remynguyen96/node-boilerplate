ARG PASSWORD=secret
ARG NAME_DB=node-course
ARG HOST=test_mysql

FROM mysql:8.0.12

ENV MYSQL_ROOT_PASSWORD=$PASSWORD MYSQL_DATABASE=$NAME_DB

EXPOSE 3306

FROM phpmyadmin/phpmyadmin

ENV PMA_HOST=$HOST MYSQL_ROOT_PASSWORD=$PASSWORD PHP_UPLOAD_MAX_FILESIZE=100MB

EXPOSE 8888
